# ai-tic-tac-toe
AI in games learning project, game is played in a terminal, need to train model first to be able to play

to train you have to first uncomment line 61:  `#self.makeMove(random.choice(self.availableMoves()), "O")`

call a function `training()` and comment/delete call for function `game()`




